[package]
name = "wifi-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.31", default-features = false, features = ["default_fonts", "glow", "x11", "wayland"] }
egui = "0.31"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
